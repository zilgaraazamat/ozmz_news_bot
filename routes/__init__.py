"""routes — HTTP-эндпоинты, разбитые на модули по предметной области.

Раньше все эндпоинты жили в одном гигантском if/elif внутри server.py.
Теперь каждый модуль — это mixin-класс с методами `route_<verb>_<name>`;
server.py просто собирает их в один Handler и диспетчеризует по словарю
путь → имя метода. Поведение каждого эндпоинта не менялось — только его
расположение в коде.
"""
